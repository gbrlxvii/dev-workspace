{
  "name": "dev-workspace",
  "version": "0.1.0",
  "description": "Dev tools and utilities",
  "scripts": {
    "test": "npm install && node test.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "env-config-helper": "github:gbrlxvii/env-config-helper"
  }
}
